<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Light">

        <activity android:name=".main.MainAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".player.PlayerAct" />
        <activity android:name=".aimation.CanvasAct" />
        <activity android:name=".test.act.TestAct" />
        <activity android:name=".test.room.TestRoomCoroutinesAct" />
        <activity android:name=".test.base.BaseAct" />

    </application>
</manifest>